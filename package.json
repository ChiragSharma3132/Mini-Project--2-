{
  "name": "mini-dbms",
  "version": "1.0.0",

"scripts": {
    "backend": "javac -cp \"lib/*;.\" backend/dbengine/*.java && java -cp \"lib/*;.\" backend.dbengine.MainServer",
    "frontend": "npx serve fontend -l 3000",
    "dev": "npx concurrently \"npm run backend\" \"npm run frontend\""
  }
}
