{
  "name": "mini-dbms",
  "version": "1.0.0",
  "scripts": {
    "backend": "javac -cp \"backend/lib/*;.\" backend/dbengine/*.java && java -cp \"backend/lib/*;.\" backend.dbengine.MainServer",
    "frontend": "npx serve FrontEnd -l 3000",
    "dev": "npx concurrently \"npm run backend\" \"npm run frontend\""
  }
}